# 任务文档

## 1. 准备阶段
- [x] 完成 `requirement.md`
- [x] 完成 `design.md`
- [x] 编码前获得用户确认

## 2. 前端任务
- [x] 更新日志弹窗参数结构，支持 `projectId`
- [x] 从数据调度列表行透传 `projectId`
- [x] 对齐 hooks/api/types 的 `projectId` 契约
- [ ] 验证日志弹窗功能不回归

## 3. 后端任务
- [x] 新增队列表真实日志读取 helper（只读）
- [x] `logs/meta`、`logs`、`logs/export` 支持 `projectId`
- [x] 保持原路径和旧参数兼容
- [x] 增加后端单元测试覆盖项目日志查询行为

## 4. 联调任务
- [x] 对齐前后端 `projectId` 传参与回退规则
- [x] 确认返回字段与前端表格显示一致

## 5. 验证
- [x] 执行后端单测
- [x] 执行后端语法校验
- [x] 执行前端 TypeScript 校验
- [ ] 记录已知限制

## 6. 交付
- [ ] 总结变更文件及其用途
- [ ] 总结验证结果
- [ ] 等待下一步指令
